const frisby = require('frisby');
const { expect } = require('chai');

describe('POST /sale', () => {
  it('', () => {});
});

describe('GET /sale', () => {
  it('', () => {});
});

describe('GET /sale/:id', () => {
  it('', () => {});
});